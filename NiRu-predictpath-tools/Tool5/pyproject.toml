[project]
name = "tool5-execution-engine"
version = "0.1.0"
description = "Controlled Response Execution Engine"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.5.0",
    "rich>=13.7.0"
]

[tool.setuptools]
packages = ["src"]
