[project]
name = "tool4-response-engine"
version = "0.1.0"
description = "Adaptive Response Prioritization & Decision Engine"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.5.0",
    "rich>=13.7.0"
]

[tool.setuptools]
packages = ["src"]
